# 阶段 0：先建立网络全局视角
学习内容：
- 客户端、服务端、局域网、互联网
- IP、端口、Socket、协议的基本概念
- 网络分层：应用层、传输层、网络层、链路层
- 数据从一台设备上的程序到另一台设备上程序的大致旅程
- 请求、连接、数据包、延迟、带宽、丢包的区别
学完应能：
- 解释“IP + 端口”为什么能定位一个网络服务。
- 从整体上解释一次游戏客户端与服务器通信会经过哪些层次。

# 阶段 1：网络诊断与观察能力
学习内容：
- ipconfig：本机网络配置
- ping：连通性与延迟
- tracert：路由路径
- nslookup：DNS 解析
- curl：测试 HTTP 接口
- 浏览器开发者工具 Network 面板
- Wireshark 基础：筛选、抓 DNS/TCP/HTTP 包
学完应能：
- 区分“服务没启动、端口没开、DNS 出错、网络不通、请求超时”。
- 抓到并初步看懂 DNS 查询、TCP 建连和 HTTP 请求。
这一阶段非常重要。多人游戏调试时，不能只靠猜“Replication 为什么没生效”。

# 阶段 2：应用层通信与 Web 服务基础
学习内容：
- HTTP 请求与响应
- URL、Header、Body、JSON
- GET、POST、状态码、超时与重试
- HTTPS、TLS、证书解决的问题
- Cookie、Session、Token、JWT 的基本区别
- WebSocket 与普通 HTTP 的区别
- 游戏常见 HTTP 使用场景：登录、公告、配置、背包、排行榜、支付回调等
学完应能：
- 理解游戏客户端为何通常同时使用 HTTP 和长连接/游戏连接。
- 能分析登录接口失败、鉴权失败、接口超时等问题。
- 知道账号、令牌、支付结果不能只信任客户端。

# 阶段 3：TCP、UDP 与实时网络基础
学习内容：
- TCP：连接、可靠、有序、字节流
- 三次握手、四次挥手
- 重传、流量控制、拥塞控制的工程直觉
- 粘包、半包与消息边界
- UDP：数据报、低额外开销、不保证送达或顺序
- 延迟、抖动、丢包、带宽对实时游戏体验的影响
- TCP、UDP、WebSocket、QUIC 的基本选型
学完应能：
- 解释 TCP 为何“可靠”，以及它为何并不总适合高频实时状态。
- 解释 UDP 丢包、乱序后会发生什么。
- 根据业务判断：登录走 HTTP、聊天走 TCP/WebSocket、实时位置信息可能走 UDP 的原因。
这里不需要一开始手写完整可靠 UDP 协议，也不需要深挖拥塞控制算法公式。

# 阶段 4：IP、局域网、路由与线上连接问题
学习内容：
- IPv4、私有 IP、公网 IP
- 子网、网关、路由的实用理解
- MAC、ARP、DHCP
- 交换机与路由器的职责
- NAT：家庭网络和公司内网如何访问外网
- 防火墙、端口开放、端口映射
- DNS、CDN、反向代理、负载均衡的用途认知
学完应能：
- 理解“局域网联机可以，公网联机不行”的常见原因。
- 初步判断联机失败发生在客户端、本地网络、NAT、防火墙还是服务器。
- 知道开发环境中的 Listen Server 与 Dedicated Server 的差别。

# 阶段 5：游戏网络设计的通用原则
这是进入 UE 前的关键过渡层。
学习内容：
- 客户端不可信：为何不能让客户端决定伤害、金钱、掉落和命中
- 服务端权威：输入/请求、校验、计算、广播结果
- 状态同步与事件同步的区别
- 高频状态、低频状态、可靠消息、不可靠消息的取舍
- 网络实体的所有权（Ownership）
- 心跳、超时、断线、重连、状态恢复
- 带宽预算与同步频率
- 可预测逻辑与必须由服务器裁决的逻辑
学完应能：
- 对任意 Gameplay 功能先做网络职责划分：客户端表现什么、服务器裁决什么、哪些数据同步。
- 明确“客户端表现可以先执行，但最终结果以服务器为准”。

UE 多人 Gameplay 专项

# 阶段 6：UE 网络框架与对象职责
学习内容：
- UE 的 Client-Server 模型
- Listen Server 与 Dedicated Server
- GameMode、GameState、PlayerState
- PlayerController、Pawn/Character、PlayerCameraManager
- Authority、Autonomous Proxy、Simulated Proxy
- Actor 生命周期与网络生成
- 网络相关的运行模式与 PIE 多人调试
学完应能：
- 知道一个 Gameplay 逻辑应放在哪个 UE 类中。
- 解释为什么 GameMode 不会复制到普通客户端，为什么 PlayerState 适合放需要公开同步的玩家状态。

# 阶段 7：Replication、属性同步与 RPC
学习内容：
- Actor Replication 的基本工作方式
- Replicated Property 与 RepNotify
- Server RPC、Client RPC、NetMulticast RPC
- RPC 的调用条件、所有权限制和可靠性取舍
- 网络变量和本地临时变量的区分
- 初始同步、增量同步、同步顺序不确定性
- 常见错误：客户端直接改同步变量、滥用 Multicast、以为 RepNotify 在所有情景都可靠触发
学完应能：
- 正确实现“客户端请求 → 服务器验证/修改状态 → 状态复制给相关客户端”。
- 不把 Replication 当成“勾选属性后自动解决所有多人问题”。

# 阶段 8：多人 Gameplay 的核心实现模式
学习内容：
- 技能、开火、受击、死亡、拾取、交互的网络职责划分
- 预测性表现与服务器最终裁决
- Ammo、HP、Cooldown、Buff、Score 等状态的同步策略
- 特效、音效、动画该本地播放、广播，还是由状态驱动
- Gameplay Ability System 的网络相关概念：预测、预测键、Ability 激活与效果同步
- Gameplay Tag / Attribute 在多人逻辑中的使用边界
学完应能：
- 设计一个多人技能时，能主动写出：客户端输入、Server RPC、服务器校验、状态同步、各客户端表现这条完整链路。
- 避免“本机看起来成功，其他玩家不一致”或“客户端可直接修改伤害”的问题。
# 阶段 9：移动、预测、插值与延迟补偿
学习内容：
- 网络延迟造成的手感问题
- 客户端预测、服务器校正
- 远端角色的插值显示
- UE Character Movement Component 的网络预测机制
- 射击与命中检测中的延迟补偿概念
- 服务器回溯（Server Rewind）的适用场景与限制
- 高延迟、抖动、丢包下的表现与降级策略
学完应能：
- 理解 UE 角色移动为何不能简单同步 Transform。
- 能解释“本地明明打中了，服务器却判定没打中”的根源和基本解决方向。
- 知道什么时候可以用 UE 原生移动同步，什么时候才需要自定义网络移动方案。

# 阶段 10：帧同步、状态同步与回滚
学习内容：
- 状态同步的适用范围、优缺点
- 帧同步/锁步同步的适用范围、优缺点
- 回滚网络代码的基本思想
- 输入同步、确定性、随机数、浮点误差
- 为什么 3D 动作游戏通常不直接使用纯帧同步
- 混合方案：局部预测、关键状态服务端权威
学完应能：
- 根据游戏类型选择同步方案，而不是把“帧同步”当成更高级的万能方案。
- 能说明动作、射击、格斗、RTS 对网络同步的不同需求。

# 阶段 11：网络优化、稳定性与安全边界
学习内容：
- Net Relevancy、Net Cull Distance、Dormancy
- 同步频率、网络优先级、带宽控制
- 大量 Actor 同步的性能问题
- 数据量压缩、只同步必要状态
- 心跳、超时、断线提示、重连和状态恢复
- Dedicated Server 下的日志与问题复现
- 基础反作弊原则：服务器校验、速率限制、输入合法性检查、异常状态检测
学完应能：
- 知道多人场景卡顿或带宽暴涨时，应从哪些同步项开始检查。
- 能为关键 Gameplay 逻辑划定客户端与服务器的安全边界。

# 阶段 12：综合验收项目
最终应通过一个小型多人玩法项目验收，例如多人射击、近战对战或技能竞技场。它至少包含：
- 玩家登录或房间连接的基础流程认知
- Dedicated Server 或 Listen Server 联机测试
- 可同步的移动、技能、受击、死亡与重生
- 客户端即时表现与服务器权威判定
- 至少一个需要预测或插值处理的交互
- 高延迟/丢包模拟下的测试与排错
- 断线、重连或超时处理
- 使用网络分析工具或 UE 网络调试工具定位一次实际问题
这条路线的核心不是把你训练成网络底层工程师，而是让你成为能独立负责“多人玩法功能”的 Gameplay 程序员：既能做玩法，也能让玩法在网络环境下正确、流畅、可维护地运行。

# 待办：小型 UE 网络同步纵切项目

在进入 UE 多人 Gameplay 实作阶段时，先创建一个独立的小型网络同步项目或测试工程，作为主 ARPG 项目的前置验证场景。该项目不是最终作品，而是用于隔离网络问题、验证可迁移的多人玩法基础设施。

建议最小范围：

- 两名玩家连接同一场景。
- 基础移动同步。
- 玩家攻击请求、服务器命中判定和 HP 同步。
- 死亡与重生状态同步。
- 一个简单敌人或简化 Boss 的受击与死亡。
- Listen Server、Dedicated Server 和 PIE 多客户端测试。
- 高延迟、丢包、断线与重连的基础验证。

完成验证后，再将通用部分迁移回主 ARPG 项目：

- 网络职责划分。
- 服务器权威的攻击与伤害接口。
- HP、死亡、重生等同步状态。
- 网络调试面板和测试流程。

主 ARPG 项目的 Boss AI、复杂攻击判定、多小怪同屏优化和核心状态转换仍由主项目单独演进，不在这个前置纵切项目中一次性全部网络化。
