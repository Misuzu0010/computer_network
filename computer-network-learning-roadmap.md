# 阶段 0：先建立网络全局视角
学习内容：
- 客户端、服务端、局域网、互联网
- IP、端口、Socket、协议的基本概念
- 网络分层：应用层、传输层、网络层、链路层
- 数据从一台设备上的程序到另一台设备上程序的大致旅程
- 请求、连接、数据包、延迟、带宽、丢包的区别
学完应能：
- 解释“IP + 端口”为什么能定位一个网络服务。
- 从整体上解释一次游戏客户端与服务器通信会经过哪些层次。

# 阶段 1：网络诊断与观察能力
学习内容：
- ipconfig：本机网络配置
- ping：连通性与延迟
- tracert：路由路径
- nslookup：DNS 解析
- curl：测试 HTTP 接口
- 浏览器开发者工具 Network 面板
- Wireshark 基础：筛选、抓 DNS/TCP/HTTP 包
学完应能：
- 区分“服务没启动、端口没开、DNS 出错、网络不通、请求超时”。
- 抓到并初步看懂 DNS 查询、TCP 建连和 HTTP 请求。
这一阶段非常重要。多人游戏调试时，不能只靠猜“Replication 为什么没生效”。

### 补充：Linux 网络诊断与抓包（优先级：P0）

**学习内容：**

- 优先级约定：P0 是负责多人玩法前必须补齐；P1 是建议随项目补齐；P2 是遇到对应需求再深入。以下补充沿用此约定。
- `ip addr`、`ip route` 看地址和路由，对照 Windows 的 `ipconfig`、`route print`；`ping` 两端通用，`mtr` 对照 `tracert`，`dig` 对照 `nslookup`。
- `ss -lntp` 看 TCP 监听，`ss -lunp` 看 UDP 绑定，`ss -tnp` 看 TCP 连接；部分进程信息需要管理员权限。UDP 没有 TCP 式 LISTEN 状态。
- `tcpdump` 抓包、`nc` 测试 TCP 连接；读懂 `iptables`/`nftables` 的入站、出站、转发和规则计数，不需要先学会编写复杂防火墙。

**为什么游戏开发需要：**

- Dedicated Server 常运行在 Linux。绑定回环地址、进程没启动、系统防火墙拦截都可能表现为“玩家进不了房间”，需要逐层定位。

**最小实验/工具：**

- 在 Linux 虚拟机或测试服务器启动本地 HTTP 服务：`python3 -m http.server 8080 --bind 0.0.0.0`；用 `ss -lntp` 确认监听，再从另一台设备运行 `curl http://服务器地址:8080/` 和 `nc -vz 服务器地址 8080`。
- 用 `ip route get 目标IP` 看出口；用 `sudo tcpdump -ni any -w network-lab.pcap 'port 53 or tcp port 8080 or tcp port 443 or udp port 7777'` 采集后交给 Wireshark。接口、端口按实验实际情况替换。
- 只读检查 `sudo nft list ruleset` 或 `sudo iptables -L -n -v`，确认实际使用的规则管理工具；不要为了排错直接清空规则。

**学完应能：**

- 按“域名解析 → 路由 → 进程绑定 → 主机/云防火墙 → 数据包到达 → 应用响应”给出证据，区分服务没启动、端口没开、DNS 出错、网络不通和请求超时。

**常见坑：**

- `ping` 失败不等于游戏端口不通；中间路由器不回应或限制 ICMP，不代表它丢弃转发流量。`mtr` 中间一跳丢包必须结合后续节点和终点判断。
- `nc -u` 没报错不能证明 UDP 服务可达；必须观察应用回复或服务端抓包。WSL2 的虚拟网络也不等同于公网服务器网络。

**可暂不深入：**

- Linux 内核网络栈源码、复杂策略路由、防火墙脚本体系；先做到会读现有配置和定位故障。

### 补充：抓包实验清单（优先级：P0）

**学习内容：**

- 区分抓包过滤器与 Wireshark 显示过滤器；前者决定采集什么，后者决定显示什么。围绕一次请求，关联时间、方向、地址、端口和协议。

**为什么游戏开发需要：**

- “没有发出”“中途没到”“服务端收到了但没处理”需要不同修复方式；抓包能把网络问题与 Gameplay 逻辑问题分开。

**最小实验/工具：**

- DNS：执行 `dig example.com A`，显示过滤 `dns`，找到问题、回答和返回码。缓存、DoH/DoT 会影响是否能看到明文 DNS。
- TCP：请求阶段 1 的 HTTP 测试服务，过滤 `tcp.port == 8080`，找到 SYN、SYN/ACK、ACK，并跟踪同一条 TCP Stream。
- HTTP：过滤 `http`，找到请求方法、路径、状态码和响应正文；非标准端口识别失败时使用 Decode As。只用无敏感数据的本地明文服务。
- UDP：运行阶段 3 的 UDP echo，过滤 `udp.port == 7777`，比较请求和响应的数据报长度、地址及端口。
- TLS：执行 `curl -v https://example.com/`，过滤 `tls`，观察 ClientHello、ServerHello 和后续加密流量；用 TLS 工具补充证书链信息。
- 每项保留一份 pcap 和几句说明：谁向谁发送、哪一步成功、还不能从抓包证明什么。

**学完应能：**

- 独立抓到并初步看懂 DNS 查询、TCP 三次握手、HTTP 请求响应、UDP 数据报和 TLS 握手的可见部分。

**常见坑：**

- TLS 1.3 的证书等握手内容通常已加密，不能要求仅凭普通抓包读出完整证书链；HTTPS 正文也不是明文 HTTP。
- 网卡校验和卸载可能让本机抓包显示“错误校验和”，不宜直接判定线上损坏。抓错接口、抓包自身丢包也会误导结论。
- 不把含令牌、账号或玩家数据的抓包直接上传公共渠道。

**可暂不深入：**

- 协议每个字段、TLS 解密配置、自动化流量分析；先掌握能支持故障判断的最小信息。

# 阶段 2：应用层通信与 Web 服务基础
学习内容：
- HTTP 请求与响应
- URL、Header、Body、JSON
- GET、POST、状态码、超时与重试
- HTTPS、TLS、证书解决的问题
- Cookie、Session、Token、JWT 的基本区别
- WebSocket 与普通 HTTP 的区别
- 游戏常见 HTTP 使用场景：登录、公告、配置、背包、排行榜、支付回调等
学完应能：
- 理解游戏客户端为何通常同时使用 HTTP 和长连接/游戏连接。
- 能分析登录接口失败、鉴权失败、接口超时等问题。
- 知道账号、令牌、支付结果不能只信任客户端。

### 补充：TLS 握手与证书链（优先级：P0）

**学习内容：**

- 理解握手大致顺序：协商版本和参数、验证服务器身份、建立会话密钥、传输加密数据；TLS 1.3 的具体消息顺序与旧版本不同，不必背状态机。
- 证书链连接服务器证书、中间证书与受信任根；验证域名/SAN、有效期、信任链。SNI 帮助服务器选择域名对应的站点和证书，ALPN 协商 `h2`、`http/1.1` 等应用协议。

**为什么游戏开发需要：**

- HTTPS 登录、支付和鉴权可能因客户端时钟错误、证书过期、缺少中间证书、代理或信任库问题失败，不能一律归因于接口代码。
- TLS 握手成功只证明连接安全条件成立，不代表账号已通过应用层鉴权；HTTP 401/403 要继续查令牌和权限。

**最小实验/工具：**

- 使用 `curl -v https://example.com/` 观察连接、证书验证和协议协商；使用 `openssl s_client -connect example.com:443 -servername example.com -showcerts -verify_hostname example.com -verify_return_error` 检查链与域名，确保工具有可用的 CA 信任库。
- 在本地测试服务使用不受信任证书或错误域名，记录失败信息，再通过正确证书、域名和测试 CA 配置修复。

**学完应能：**

- 区分 DNS/TCP 失败、TLS 验证失败与 HTTP 鉴权失败；能查证书有效期、SAN、链是否完整，以及客户端系统时间。

**常见坑：**

- 用 IP 访问域名证书可能验证失败；只看浏览器成功不能证明 UE 客户端信任库也正确。`openssl s_client` 的输出需要结合验证结果，不能只看“已连接”。
- 不把关闭证书验证或 `curl -k` 当成正式修复；HMAC、JWT 也不能替代 TLS 的传输保护。

**可暂不深入：**

- 加密数学、TLS 完整状态机、自研 TLS；证书固定和双向 TLS 等遇到实际需求再学。

### 补充：HTTP/2、HTTP/3、QUIC、WebSocket 握手与长连接选型（优先级：P1）

**学习内容：**

- “普通 HTTP”不是另一套业务语义：HTTP/1.1、HTTP/2、HTTP/3 仍是请求与响应。HTTP/2 在 TCP 上多路复用；HTTP/3 基于 QUIC，QUIC 基于 UDP 并集成 TLS 安全机制。
- HTTP/2 仍受底层 TCP 丢包的队头阻塞影响；QUIC 可减轻不同流之间的这种阻塞，同一可靠流仍需按序交付，并非完全没有等待。
- 常见 WebSocket 经 HTTP/1.1 Upgrade 握手，成功响应为 101，随后双向传消息；HTTP/2、HTTP/3 的扩展 CONNECT 机制不同。掌握 `wss`、Ping/Pong、应用心跳、超时和带退避的重连。

**为什么游戏开发需要：**

- 登录、公告、配置适合 HTTP；聊天和大厅通知可用 WebSocket；高频实时状态通常使用 UE 既有游戏传输或明确设计的 UDP 方案。协议选错会带来排队延迟、连接频繁断开和无效重试。

**最小实验/工具：**

- 用浏览器 Network 面板检查 HTTP 版本和 WebSocket 消息；用 `curl --http2 -v` 测试支持 HTTP/2 的服务。HTTP/3 实验先以 `curl -V` 确认客户端支持，再访问支持 HTTP/3 的测试服务。
- 在本地 WebSocket echo 服务断开网络后恢复，观察心跳超时、重连退避和消息是否重复；记录代理的空闲连接超时。

**学完应能：**

- 按下表解释选择，而不是认为“基于 UDP 就一定更快”。

| 方案 | 适合的需求 | 需要承担的工作 |
| --- | --- | --- |
| TCP | 自定义可靠有序控制消息 | 消息分帧、超时、积压控制；丢包会阻塞后续字节 |
| UDP | 可丢弃旧数据的实时状态 | 包大小、拥塞与速率控制、必要的可靠性和安全机制 |
| WebSocket | 聊天、大厅推送、浏览器双向通信 | 心跳、鉴权、代理兼容、重连与业务去重；常见实现走 TCP |
| QUIC | 有成熟库支持的多流与迁移需求 | 库和部署支持、UDP 可达性；不等于 UE 开箱即用的替代传输 |
| HTTP/2 或 HTTP/3 | 登录、配置、公告等 HTTP 服务 | 接口幂等、鉴权、超时与网关兼容 |

**常见坑：**

- HTTP/3 不会自动把 HTTP 变成任意不可靠游戏消息通道；QUIC 的 DATAGRAM 扩展也需要双方与应用支持。
- WebSocket 自动重连不等于会话恢复；无限重试、重发非幂等请求可能放大故障或重复扣款。

**可暂不深入：**

- QUIC 协议实现、HTTP 压缩表细节、WebTransport；这些列为 P2，需求明确后再做原型。

# 阶段 3：TCP、UDP 与实时网络基础
学习内容：
- TCP：连接、可靠、有序、字节流
- 三次握手、四次挥手
- 重传、流量控制、拥塞控制的工程直觉
- 粘包、半包与消息边界
- UDP：数据报、低额外开销、不保证送达或顺序
- 延迟、抖动、丢包、带宽对实时游戏体验的影响
- TCP、UDP、WebSocket、QUIC 的基本选型
学完应能：
- 解释 TCP 为何“可靠”，以及它为何并不总适合高频实时状态。
- 解释 UDP 丢包、乱序后会发生什么。
- 根据业务判断：登录走 HTTP、聊天走 TCP/WebSocket、实时位置信息可能走 UDP 的原因。
这里不需要一开始手写完整可靠 UDP 协议，也不需要深挖拥塞控制算法公式。

### 补充：Socket API 与 TCP/UDP 编程（优先级：P0）

**学习内容：**

- TCP 服务端：`socket → bind → listen → accept → recv/send → close`；客户端：`socket → connect → send/recv → close`。`accept` 返回新的连接 Socket，监听 Socket 继续接客。
- UDP：`socket → bind → recvfrom/sendto`，没有 `listen/accept`；UDP 的 `connect` 通常只是设置默认对端和接收过滤，不进行 TCP 式握手。
- 阻塞与非阻塞、操作超时、可读可写通知；非阻塞 `connect` 需要等待完成并检查错误。端口复用选项与 TCP Keepalive 的基本用途。
- TCP 是字节流：用长度前缀或分隔符定义消息边界，处理部分发送和分段接收；TCP `recv` 返回 0 表示对端有序关闭，UDP 则允许零长度数据报。

**为什么游戏开发需要：**

- 亲手建一次连接才能理解“端口没开”“连接超时”“连接被拒绝”的差别，也能避免把后台 HTTP/Socket 阻塞放到游戏主线程造成卡顿。

**最小实验/工具：**

- 用熟悉的 C++ 或 Python 标准库写 TCP echo 与 UDP echo：先本机、再两台设备互发。TCP echo 客户端按预期总字节数接收；UDP echo 按单个数据报回传，并带请求序号和接收超时。
- 依次测试正常服务、停止服务、错误地址、仅绑定 `127.0.0.1`、实验环境防火墙丢弃，联合错误码、`ss` 和抓包记录结果。
- TCP 加一次连续发送两条消息和一次大消息实验，证明一次 `send` 不对应一次 `recv`；设置合理的最大消息长度。

**学完应能：**

- 解释拒绝常见于 TCP RST/主动拒绝，超时说明截止时间前未完成但不能单独确定故障点；知道 UDP 没有统一的“连接成功”信号。
- 清理连接资源，检查返回值，区分监听 Socket 与连接 Socket，不靠无限阻塞等待判定存活。

**常见坑：**

- `SO_REUSEADDR`、`SO_REUSEPORT` 在不同系统上的语义不同，不能当成“允许多个服务随意占同一端口”；Windows 还需认识独占绑定选项。
- Windows 原生 C++ 需要 Winsock 初始化、`closesocket` 和对应错误码；POSIX 示例不能原样照搬。TCP Keepalive 不是应用业务心跳。

**可暂不深入：**

- 高并发 IO 框架、IOCP/epoll 实现、完整可靠 UDP；先把两个 echo 和错误路径跑通。

### 补充：TCP 工程细节（优先级：P0）

**学习内容：**

- Nagle 合并部分小写入，延迟 ACK 减少确认包；特定交互下两者可能增加小消息等待。`TCP_NODELAY` 可禁用 Nagle，但不是所有延迟的解药。
- 滑动窗口用于流量控制，反映接收能力；拥塞控制根据网络反馈限制发送速率。理解接收慢、网络堵、应用没及时发送是不同问题。
- TIME_WAIT 通常出现在主动关闭方，帮助处理旧报文与最终确认重传；TCP Keepalive 探测空闲连接，默认探测周期可能很长。

**为什么游戏开发需要：**

- 聊天小包延迟、登录连接卡顿、服务器大量短连接、客户端临时端口或 NAT 映射资源耗尽，都需要这些工程直觉。

**最小实验/工具：**

- 给 TCP echo 加上分开写入的小请求，测应用往返时间，比较启用/禁用 `TCP_NODELAY` 的结果，并用抓包确认何处等待；未观察到差异也如实记录。
- 在本地有限次数地建立/关闭连接，用 `ss -tan state time-wait` 看状态；让接收端暂停读取，用 `ss -tin`、抓包观察队列和接收窗口。
- 设置测试用 Keepalive 与应用心跳，比较对端正常关闭和网络黑洞时的发现时间。

**学完应能：**

- 面对卡顿先看应用发送时间、队列、重传、接收窗口与 RTT，再决定是否改 Socket 参数；面对短连接压力先考虑连接复用和资源限制。

**常见坑：**

- 大量 TIME_WAIT 不自动等于故障，也不必然耗尽服务端监听端口；要确认是哪一端、哪类地址/端口或 NAT 资源达到限制。
- 不靠激进修改系统 TCP 参数掩盖连接泄漏；TCP 连接未断开不代表业务线程仍健康。

**可暂不深入：**

- 拥塞控制公式、不同算法的数学推导和内核调参大全；先能解释现象并选择排查方向。

### 补充：MTU、MSS、IP 分片（优先级：P0）

**学习内容：**

- MTU 限制一跳可承载的 IP 包大小，路径 MTU 取决于路径上的限制；TCP MSS 是 TCP 数据部分的大小约束，不能直接当作 UDP 负载上限。
- UDP 负载还要扣除 IP、UDP 以及实际传输中的封装开销。VPN、隧道可能进一步缩小可用空间。
- IPv4 可能分片，也可能因 DF 标志被丢弃；IPv6 路由器不做分片。路径 MTU 发现依赖相关反馈，被屏蔽可能形成“大包黑洞”。

**为什么游戏开发需要：**

- 小包能通、大包丢失，TCP 建连成功但传数据卡住，常与路径 MTU 有关。UDP 分片中任意一片丢失都会影响整个数据报交付。

**最小实验/工具：**

- 对支持 ICMP 的测试目标用 Linux `ping -M do -s 负载大小 目标IP` 或 Windows `ping -f -l 负载大小 目标IP` 探测 IPv4，结合 `tracepath`；ICMP 负载大小不等于 IP 包大小。
- 给 UDP echo 逐步增加负载，例如 256、512、1000、1200、1400 字节；在虚拟机测试链路降低 MTU，记录送达、分片、ICMP 反馈或本地发送错误。

**学完应能：**

- 为协议制定包大小预算，优先拆分或减少大状态消息；把约 1200 字节看作常见保守起点而非所有网络的保证，并按实际协议与路径验证。

**常见坑：**

- UDP 理论最大数据报长度不是推荐游戏包大小；`sendto` 成功只说明本机接受数据，不保证网络送达。
- 不直接修改 UE 包大小配置来“修复丢包”；先确认实际 NetDriver、封装开销和抓包现象。

**可暂不深入：**

- 分片字段背诵、复杂探测算法；先理解预算、路径变化和黑洞排查。

# 阶段 4：IP、局域网、路由与线上连接问题
学习内容：
- IPv4、私有 IP、公网 IP
- 子网、网关、路由的实用理解
- MAC、ARP、DHCP
- 交换机与路由器的职责
- NAT：家庭网络和公司内网如何访问外网
- 防火墙、端口开放、端口映射
- DNS、CDN、反向代理、负载均衡的用途认知
学完应能：
- 理解“局域网联机可以，公网联机不行”的常见原因。
- 初步判断联机失败发生在客户端、本地网络、NAT、防火墙还是服务器。
- 知道开发环境中的 Listen Server 与 Dedicated Server 的差别。

### 补充：NAT 类型与 STUN/TURN/ICE 穿透（优先级：P0）

**学习内容：**

- 用传统四分类建立直觉：Full Cone 对已建立的映射较少限制外部来源；Restricted 限制为内部曾访问过的外部 IP；Port Restricted 进一步限制外部端口；Symmetric 的映射会随目标变化，通常更难直接打洞。
- 四分类是简化模型，真实 NAT 应结合映射行为、过滤行为、超时、双重 NAT、运营商 CGNAT 和 hairpin 支持判断，不能只凭标签断言能否穿透。
- STUN 帮助发现服务器所观察到的公网映射；TURN 提供流量中继；ICE 收集候选地址、做连通性检查并选择路径。UPnP 可请求支持它的本地路由器建立映射，但不保证跨越上游 NAT。

**为什么游戏开发需要：**

- Listen Server 局域网可连但公网失败、P2P 打洞失败，经常是入站路径不存在；公网 Dedicated Server 通常通过明确的公网入口与防火墙规则降低这类复杂度。

**最小实验/工具：**

- 两台设备分别处于家庭网络和手机热点，使用成熟 ICE/WebRTC 测试客户端及有权限的 STUN/TURN 服务，记录 host、srflx、relay 候选及最终选中的路径。
- 在拥有路由器管理权限的实验环境对测试服务做端口映射，再从外网验证；比较路由器 WAN 地址与外部观察地址，排查上游 NAT。记录中继路径 RTT 与流量成本。

**学完应能：**

- 解释“知道公网 IP”为何仍不等于可入站；为 Listen Server 设计直连失败后的中继或平台会话方案，为 Dedicated Server 检查真实公网入口。

**常见坑：**

- STUN 不是中继，打洞不是保证；同一局域网访问公网地址失败也可能是缺少 NAT 回环。
- ICE 测试成功不代表 UE 默认 NetDriver 自动具备 ICE；需要确认 EOS、Steam 或其他 Online Subsystem/传输插件实际提供了什么。

**可暂不深入：**

- 自研 ICE、打洞时序优化、NAT 全部行为规范；先会验证路径并使用成熟方案。

### 补充：DNS 记录类型与 SRV（优先级：P1）

**学习内容：**

- A 对应 IPv4，AAAA 对应 IPv6，CNAME 是别名，TXT 常用于域名验证和元数据，SRV 提供服务目标、端口、优先级和权重；认识 TTL 与缓存。
- 客户端通常请求递归解析器获得最终结果，递归解析器必要时通过迭代查询沿委派链查找；不要求背所有 DNS 报文结构。
- 登录/匹配接口可用域名定位；支持 SRV 的自定义客户端可查询 `_服务名._udp.域名` 等记录。动态游戏房间一般由匹配服务返回具体端点和入场票据。

**为什么游戏开发需要：**

- 地址变更、缓存未过期、错误 AAAA、内外网解析不同，会造成“部分玩家登不上”。把 IP 写死会妨碍迁移和排错。

**最小实验/工具：**

- 用 `dig example.com A`、`dig example.com AAAA` 观察地址与 TTL；在本地 DNS 测试区配置 CNAME、TXT 和 `_game._udp.lab.test` 的 SRV，再用 `dig @测试DNS地址 _game._udp.lab.test SRV` 查询。
- 用 `dig +trace example.com` 观察委派；若网络不允许直接访问外部 DNS，则记录限制，不把失败当成域名不存在。

**学完应能：**

- 区分不存在的记录、解析器失败、缓存旧地址与连接目标失败；说明 SRV 如何参与服务发现，以及匹配 API 为何通常更适合动态房间。

**常见坑：**

- UE 的普通地址连接、浏览器和通用 HTTP 客户端不会因为配置了 SRV 就自动按它连接；应用必须明确支持查询与选择。
- TXT 不适合存放密钥；DNS 解析成功不代表端口可达，SRV 的目标也仍需地址解析与连接验证。

**可暂不深入：**

- 自建生产权威 DNS、DNSSEC 实现、复杂地理调度；SRV 客户端完整实现按需求再做。

### 补充：云网络与线上部署（优先级：P0）

**学习内容：**

- VPC 是隔离的虚拟网络，子网和路由表决定可达路径；安全组、主机防火墙共同控制流量，部分平台还有子网 ACL。
- NAT 网关通常为私网实例提供主动出站访问，不自动提供任意公网入站服务。公网 IP、路由与入站映射是另外的条件。
- 区分四层 TCP/UDP 负载均衡（如部分 SLB 产品）、七层 HTTP 反向代理和 CDN；普通 HTTP CDN 不能直接承载任意 UE 游戏 UDP 流量。

**为什么游戏开发需要：**

- Dedicated Server 在云上启动成功仍可能因绑定地址、安全组协议、子网路由或负载均衡类型错误无法加入；私网地址不能直接发给公网玩家。

**最小实验/工具：**

- 在已有授权测试环境先部署 echo，再部署测试 Dedicated Server；记录进程监听地址、实际游戏/查询端口、公网入口、路由和安全组，使用外网客户端验证。
- UE 常见游戏端口是 UDP 7777，但以项目与启动参数为准。先用 `ss -lunp` 和两端抓包确认，再逐项验证安全组与主机防火墙。
- 画出“客户端 → 公网入口 → 实例或负载均衡 → 游戏进程”的路径；核对负载均衡是否支持 UDP、如何保持同一会话落到同一游戏实例、空闲超时与健康检查是否合适。无云环境时先用虚拟机验证绑定和防火墙部分。

**学完应能：**

- 给出最小部署清单，解释为什么仅打开 TCP 7777 无法解决 UDP 入站，以及为什么私有子网加 NAT 网关不等于公开了游戏服。

**常见坑：**

- 不把有状态对局当成无状态 HTTP 请求随意轮询；健康检查通过不一定表示游戏进程能接纳玩家。
- 容器还需检查端口发布和协议；不要为了排错对公网开放全部端口。实际新增付费云资源应先明确成本与授权。

**可暂不深入：**

- Kubernetes、多地域调度、复杂 VPC 互联与大规模编排；先把单实例从公网连通并留下证据。

# 阶段 5：游戏网络设计的通用原则
这是进入 UE 前的关键过渡层。
学习内容：
- 客户端不可信：为何不能让客户端决定伤害、金钱、掉落和命中
- 服务端权威：输入/请求、校验、计算、广播结果
- 状态同步与事件同步的区别
- 高频状态、低频状态、可靠消息、不可靠消息的取舍
- 网络实体的所有权（Ownership）
- 心跳、超时、断线、重连、状态恢复
- 带宽预算与同步频率
- 可预测逻辑与必须由服务器裁决的逻辑
学完应能：
- 对任意 Gameplay 功能先做网络职责划分：客户端表现什么、服务器裁决什么、哪些数据同步。
- 明确“客户端表现可以先执行，但最终结果以服务器为准”。

### 补充：序列化、压缩、加密与防重放（优先级：P0）

**学习内容：**

- JSON 便于调试，protobuf 等提供结构化编码，自定义二进制可紧凑但维护成本更高；认识协议版本、字节序、长度上限、字段兼容和解析失败处理。
- 压缩减少数据量但消耗 CPU，小包未必受益；序列化不是加密，压缩也不是加密。传输机密性和完整性优先依赖成熟 TLS 或合适的安全传输方案。
- HMAC 用共享密钥验证完整性和来源，数字签名用私钥签名、公钥验证；二者都不自动隐藏内容。nonce、时间戳、序号和服务端去重记录共同服务于防重放。
- 速率限制、输入范围与状态合法性检查、幂等请求标识；游戏伤害、物品、账号权限和支付结果由可信服务端验证。

**为什么游戏开发需要：**

- 畸形长度可能耗尽内存，重复请求可能重复发奖，合法格式的作弊输入仍可能破坏 Gameplay；能解包不等于应该执行。

**最小实验/工具：**

- 为测试消息设计带版本号、请求 ID 和长度限制的编码，用 JSON 与一种二进制方案比较大小；测试未知字段、截断消息、超大长度和非法数值。
- 用本地假账号接口连续提交同一个请求 ID，验证只执行一次并返回一致结果；篡改受认证消息、重发过期票据，确认被拒绝。用成熟库演示 HMAC，不自研密码算法。

**学完应能：**

- 区分“解析成功、认证成功、有权限、符合当前游戏状态”四道检查，说明令牌和支付回调的验证边界，以及重复奖励为何需要业务幂等。

**常见坑：**

- 客户端持有的共享密钥不能证明玩家诚实，不能给伤害包加 HMAC 后就信任客户端伤害值。
- 只加时间戳仍允许时间窗口内重放；nonce 若不验证唯一性也无效。去重存储要有期限和容量限制。
- 不压缩不可信的无限大输入，也要限制解压后大小；同一压缩上下文混合秘密与攻击者可控内容可能泄露信息。TLS 1.3 的 0-RTT 数据有重放风险，不宜直接承载扣款等操作。

**可暂不深入：**

- 密码学数学、自研加密协议、极限位打包；先用成熟库和服务器权威规则守住边界。

### 补充：房间、会话、匹配服务器架构（优先级：P1）

**学习内容：**

- 登录服确认身份；房间服管理成员与准备状态；匹配服务组织玩家并申请游戏实例；游戏服运行权威对局；网关处理接入或转发；中继服转发流量以解决可达性问题。
- 房间、账号会话、游戏对局与底层网络连接不是同一对象；它们的生命周期和标识需要分开理解。
- 典型流程：登录取凭证 → 创建/加入房间 → 匹配或分配实例 → 获得游戏端点与短期入场票据 → 游戏服验票 → 加入对局。会话恢复与重连令牌见阶段 9 补充。

**为什么游戏开发需要：**

- “匹配成功但进不了场”“重连变成第二个玩家”“游戏服退出但大厅仍显示可加入”通常是服务之间的状态和生命周期没有对齐。

**最小实验/工具：**

- 用本地简单 HTTP 服务或静态测试响应模拟登录、房间和分配接口，返回测试游戏服地址与有期限的票据；先画时序图，不需要先搭建完整微服务集群。
- 模拟实例未就绪、票据过期、连续点击加入、分配后客户端退出，列出各自应该返回的结果和清理责任。

**学完应能：**

- 画出客户端发现并连接游戏服的全过程，标出身份、房间 ID、对局 ID、端点和票据在哪里产生、验证和失效。

**常见坑：**

- 网关、中继和游戏服职责不同，中继不替代权威判定；UE Session/Online Subsystem 也不等于已包含全部登录、匹配和托管能力。
- 不把永久账号令牌作为可公开分享的房间口令，不信任客户端自报的玩家身份。

**可暂不深入：**

- 大规模匹配算法、服务发现集群、分布式一致性系统；小项目可合并服务进程，但保留清晰职责。

UE 多人 Gameplay 专项

# 阶段 6：UE 网络框架与对象职责
学习内容：
- UE 的 Client-Server 模型
- Listen Server 与 Dedicated Server
- GameMode、GameState、PlayerState
- PlayerController、Pawn/Character、PlayerCameraManager
- Authority、Autonomous Proxy、Simulated Proxy
- Actor 生命周期与网络生成
- 网络相关的运行模式与 PIE 多人调试
学完应能：
- 知道一个 Gameplay 逻辑应放在哪个 UE 类中。
- 解释为什么 GameMode 不会复制到普通客户端，为什么 PlayerState 适合放需要公开同步的玩家状态。

# 阶段 7：Replication、属性同步与 RPC
学习内容：
- Actor Replication 的基本工作方式
- Replicated Property 与 RepNotify
- Server RPC、Client RPC、NetMulticast RPC
- RPC 的调用条件、所有权限制和可靠性取舍
- 网络变量和本地临时变量的区分
- 初始同步、增量同步、同步顺序不确定性
- 常见错误：客户端直接改同步变量、滥用 Multicast、以为 RepNotify 在所有情景都可靠触发
学完应能：
- 正确实现“客户端请求 → 服务器验证/修改状态 → 状态复制给相关客户端”。
- 不把 Replication 当成“勾选属性后自动解决所有多人问题”。

# 阶段 8：多人 Gameplay 的核心实现模式
学习内容：
- 技能、开火、受击、死亡、拾取、交互的网络职责划分
- 预测性表现与服务器最终裁决
- Ammo、HP、Cooldown、Buff、Score 等状态的同步策略
- 特效、音效、动画该本地播放、广播，还是由状态驱动
- Gameplay Ability System 的网络相关概念：预测、预测键、Ability 激活与效果同步
- Gameplay Tag / Attribute 在多人逻辑中的使用边界
学完应能：
- 设计一个多人技能时，能主动写出：客户端输入、Server RPC、服务器校验、状态同步、各客户端表现这条完整链路。
- 避免“本机看起来成功，其他玩家不一致”或“客户端可直接修改伤害”的问题。
# 阶段 9：移动、预测、插值与延迟补偿
学习内容：
- 网络延迟造成的手感问题
- 客户端预测、服务器校正
- 远端角色的插值显示
- UE Character Movement Component 的网络预测机制
- 射击与命中检测中的延迟补偿概念
- 服务器回溯（Server Rewind）的适用场景与限制
- 高延迟、抖动、丢包下的表现与降级策略
学完应能：
- 理解 UE 角色移动为何不能简单同步 Transform。
- 能解释“本地明明打中了，服务器却判定没打中”的根源和基本解决方向。
- 知道什么时候可以用 UE 原生移动同步，什么时候才需要自定义网络移动方案。

### 补充：弱网模拟与弱网测试（优先级：P0）

**学习内容：**

- 分开测试延迟、抖动、随机/突发丢包、乱序、重复、带宽限制与完全断线；区分上行、下行和双向影响，记录基线 RTT。
- Windows 可用 clumsy，Linux 用 `tc netem`，macOS 可用 Network Link Conditioner；UE 自带网络模拟适合快速检查玩法，具体入口与参数按引擎版本确认。

**为什么游戏开发需要：**

- 本机 PIE 的低延迟会掩盖 RPC 积压、重复特效、预测回滚、插值缓冲不足和重连失败；必须观察客户端手感与服务端最终状态是否同时合格。

**最小实验/工具：**

- 在独立测试虚拟机的已确认网卡上，可用 `sudo tc qdisc add dev eth0 root netem delay 80ms 20ms loss 2%` 注入出站延迟与丢包；仅在无原有根规则的测试网卡使用，实验后用 `sudo tc qdisc del dev eth0 root` 移除自己添加的规则。不要在远程管理依赖的网卡上盲目操作。
- 最小测试矩阵：正常基线；额外单向 80ms 延迟；延迟加 20ms 抖动和 2% 丢包；更高延迟与 5% 丢包；断网 5 秒后恢复。明确参数是单向注入还是最终测得 RTT，双向需分别配置或使用对应工具。
- 用两个独立客户端连接 Dedicated Server，记录输入到表现、服务器校正次数/幅度、关键状态一致性、重复事件、重连耗时；每个场景重复数轮。

**学完应能：**

- 验证预测是否最终收敛、远端插值是否可接受、伤害/奖励是否只结算一次、断线后是否正确恢复或明确失败，并附可复现条件。

**常见坑：**

- 在 UE 和系统工具同时加延迟可能重复注入；netem 默认作用于指定接口出站，不能默认已经模拟双向。
- 不把服务器低帧率误认成网络丢包；过滤目标端口，避免把登录、管理连接一起扰乱后无法判断原因。

**可暂不深入：**

- 运营商级流量模型、复杂概率分布；先把固定矩阵和一次真实故障复现做扎实。

### 补充：连接迁移、会话恢复、重连令牌（优先级：P1）

**学习内容：**

- Wi-Fi/热点切换、NAT 映射到期会改变地址或端口。TCP 通常需要重建连接；QUIC 在双方实现与策略支持时可通过连接标识和路径验证支持迁移，UE 普通连接不会自动获得此能力。
- 区分重建传输连接、恢复身份、重新绑定玩家席位、恢复权威状态；设计“已连接 → 疑似断线 → 等待重连 → 恢复/过期”的状态与超时。
- 重连令牌应不可猜测、短期有效、绑定玩家和对局，在使用后轮换或按策略失效；传输要安全，日志要脱敏，不能替代服务端身份与权限校验。

**为什么游戏开发需要：**

- 玩家短暂掉线后希望回到同一角色；仅再次连接可能创建重复 Pawn、重复发奖、恢复错误背包，或让旧连接继续控制角色。

**最小实验/工具：**

- 在测试对局断网后恢复，再做一次切换网络；记录是否保留席位、旧连接何时失效、重连后收到的权威快照与序号。
- 测试令牌过期、重复使用、错误对局、两个客户端争用同一身份，以及游戏服进程已退出；分别确认拒绝、替换或提示规则。

**学完应能：**

- 画出重连时序：验证令牌和席位 → 按规则处理旧连接 → 发送权威状态 → 恢复输入；避免盲目重放断线期间全部操作，给非幂等请求明确去重规则。
- 明确重连只能恢复服务器仍保留或已持久化的状态；进程崩溃后的恢复需要另外设计，不承诺凭令牌恢复一切。

**常见坑：**

- IP 不是稳定身份；客户端存档不能覆盖服务器当前状态。重连成功也不能假设旧 Actor 引用和 RPC 队列仍有效。
- 无限快速重试会放大故障，应有退避、随机抖动和截止时间；客户端系统时间不能单独决定令牌是否有效。

**可暂不深入：**

- 跨区无缝迁移、游戏服热迁移、崩溃后完整对局恢复；先支持有限时间内返回同一实例。

# 阶段 10：帧同步、状态同步与回滚
学习内容：
- 状态同步的适用范围、优缺点
- 帧同步/锁步同步的适用范围、优缺点
- 回滚网络代码的基本思想
- 输入同步、确定性、随机数、浮点误差
- 为什么 3D 动作游戏通常不直接使用纯帧同步
- 混合方案：局部预测、关键状态服务端权威
学完应能：
- 根据游戏类型选择同步方案，而不是把“帧同步”当成更高级的万能方案。
- 能说明动作、射击、格斗、RTS 对网络同步的不同需求。

# 阶段 11：网络优化、稳定性与安全边界
学习内容：
- Net Relevancy、Net Cull Distance、Dormancy
- 同步频率、网络优先级、带宽控制
- 大量 Actor 同步的性能问题
- 数据量压缩、只同步必要状态
- 心跳、超时、断线提示、重连和状态恢复
- Dedicated Server 下的日志与问题复现
- 基础反作弊原则：服务器校验、速率限制、输入合法性检查、异常状态检测
学完应能：
- 知道多人场景卡顿或带宽暴涨时，应从哪些同步项开始检查。
- 能为关键 Gameplay 逻辑划定客户端与服务器的安全边界。

### 补充：可观测性与线上排错（优先级：P0）

**学习内容：**

- 日志记录连接阶段、房间/对局 ID、脱敏玩家 ID、请求 ID、错误码、版本与时间；trace 串联登录、匹配、分配、加入的调用链。
- 监控 RTT、抖动、协议可测的丢包/重传、连接数、收发速率、队列，以及服务器 Tick 时间、CPU、内存和网卡流量；指标必须说明来源、窗口和含义。
- UE 使用日志、Unreal Insights 的 Networking Insights 或适用版本的网络分析工具，结合 Actor 是否复制、Relevancy、Dormancy、Ownership 和 RPC 方向排查。

**为什么游戏开发需要：**

- 线上没有编辑器可随手单步，需要用证据区分服务器卡顿、链路问题、鉴权失败与 Replication 条件不满足，而不是只猜“网络同步坏了”。

**最小实验/工具：**

- 启动独立 Dedicated Server 和两个独立客户端，为一次技能标记请求 ID，关联客户端输入、Server RPC 接收、服务器校验结果、权威状态变化和客户端表现。
- 人为制造一次所有权错误或不满足同步条件的问题，另做一次弱网故障；保存相同版本的启动参数、日志、UE trace、网络条件与最短复现步骤，对比证据。
- 为登录/加入成功率、加入耗时及服务器 Tick 时间建立简单统计；有条件时使用系统监控或 Prometheus/Grafana，不要求先搭完整平台。

**学完应能：**

- 提交一份故障报告：症状、影响范围、时间线、证据、根因、修复和复测结果；从玩家 ID/对局 ID 找到对应游戏服及请求链路。

**常见坑：**

- 各机器时钟不一致会误导时间线；跨机单向延迟不能直接拿未同步的时间戳相减。RTT 与本机耗时优先使用合适的单调时钟计量。
- ICMP 丢包不是游戏协议丢包；抓包丢包也不是链路丢包。没有序号、ACK 或对应遥测时，不应伪造精确丢包率。
- 不记录令牌和完整敏感载荷；逐帧海量日志会影响性能，指标标签也不宜无限使用玩家 ID 造成高基数。

**可暂不深入：**

- 大型分布式追踪平台、eBPF、复杂告警系统；先让单局、单技能、单次加入问题可定位。

# 阶段 12：综合验收项目
最终应通过一个小型多人玩法项目验收，例如多人射击、近战对战或技能竞技场。它至少包含：
- 玩家登录或房间连接的基础流程认知
- Dedicated Server 或 Listen Server 联机测试
- 可同步的移动、技能、受击、死亡与重生
- 客户端即时表现与服务器权威判定
- 至少一个需要预测或插值处理的交互
- 高延迟/丢包模拟下的测试与排错
- 断线、重连或超时处理
- 使用网络分析工具或 UE 网络调试工具定位一次实际问题
这条路线的核心不是把你训练成网络底层工程师，而是让你成为能独立负责“多人玩法功能”的 Gameplay 程序员：既能做玩法，也能让玩法在网络环境下正确、流畅、可维护地运行。

### 补充：综合项目新增验收项（优先级：P0）

**学习内容：**

- 把新增知识接到现有多人项目的登录、加入、Gameplay、断线与诊断链路；P0 全部验收，P1 至少完成方案说明和最小实验，P2 不作为结项前置条件。

**为什么游戏开发需要：**

- 单独记住工具命令不等于能交付多人功能；验收要证明出现故障时能定位、关键状态能保持一致、连接恢复行为有明确边界。

**最小实验/工具：**

- [ ] 完成 TCP/UDP echo，附正常、拒绝、超时、错误绑定的对照记录，以及 TCP 分帧与部分收发验证。
- [ ] 用 `ss` 检查端口，提交 DNS/TCP/HTTP/UDP/TLS 五类抓包说明；能结合 TLS 工具判断证书或握手失败。
- [ ] 提交 TCP 小包延迟、TIME_WAIT、MTU/UDP 大包实验记录，区分观察事实与推测。
- [ ] 给出 HTTP/2、HTTP/3、WebSocket 与游戏传输选型；说明 DNS 各记录、SRV 支持条件及实际服务发现流程。
- [ ] 在独立客户端与 Dedicated Server 上验收；有公网测试条件时，从另一网络实际加入，并核对 VPC、路由、安全组、系统防火墙与监听端口。只有内网实验时明确标记公网项尚未验证。
- [ ] 给出 NAT/STUN/TURN/ICE 路径说明和直连失败的后备方案，注明 UE 所用平台/插件是否真正支持。
- [ ] 串起登录或测试身份、房间、实例地址、验票、加入流程；验证协议长度上限、非法输入、重复请求、过期票据和速率限制。
- [ ] 按弱网矩阵测试移动、技能、受击、死亡、重生，记录状态一致性、校正和重复事件；阈值由游戏目标事先定义，不照搬统一毫秒标准。
- [ ] 断线后在允许时间内恢复同一玩家状态，超时明确失败；验证旧连接处理、令牌失效与非幂等操作去重。
- [ ] 提交一次可复现的排错报告，附脱敏日志、指标、trace/抓包及复测结果。

**学完应能：**

- 用可复现材料证明“客户端输入 → Server RPC → 服务器校验 → 状态同步 → 各客户端表现”全链路正确，并解释弱网与部署故障发生在哪一层。

**常见坑：**

- 不把单进程 PIE 通过等同于公网交付；工具不支持或环境不足的项目标记未验证，不能勾选为已完成。
- 验收要包含独立进程、不同客户端视角与失败路径；不要求为了这一项目先自研匹配平台或传输协议。

**可暂不深入：**

- 大规模压测、多地域容灾和完整商业后端；先交付小规模、可诊断、可恢复的多人玩法纵切。

# 待办：小型 UE 网络同步纵切项目

在进入 UE 多人 Gameplay 实作阶段时，先创建一个独立的小型网络同步项目或测试工程，作为主 ARPG 项目的前置验证场景。该项目不是最终作品，而是用于隔离网络问题、验证可迁移的多人玩法基础设施。

建议最小范围：

- 两名玩家连接同一场景。
- 基础移动同步。
- 玩家攻击请求、服务器命中判定和 HP 同步。
- 死亡与重生状态同步。
- 一个简单敌人或简化 Boss 的受击与死亡。
- Listen Server、Dedicated Server 和 PIE 多客户端测试。
- 高延迟、丢包、断线与重连的基础验证。

完成验证后，再将通用部分迁移回主 ARPG 项目：

- 网络职责划分。
- 服务器权威的攻击与伤害接口。
- HP、死亡、重生等同步状态。
- 网络调试面板和测试流程。

主 ARPG 项目的 Boss AI、复杂攻击判定、多小怪同屏优化和核心状态转换仍由主项目单独演进，不在这个前置纵切项目中一次性全部网络化。

# 最小补充包

如果时间有限，先补这 8 组。P0 以能做实验和解释失败为完成标准；其中涉及的 P1 内容先认识边界，不要求搭完整平台。

1. **P0：Linux 诊断与五类抓包。** 会用 `ip`、`ss`、`tcpdump`、`dig`，抓到 DNS/TCP/HTTP/UDP/TLS，并能区分解析、连接和应用失败。
2. **P0：Socket 与 TCP 工程直觉。** 写 TCP/UDP echo，理解消息边界、部分收发、超时、Nagle、延迟 ACK、TIME_WAIT 和 Keepalive。
3. **P0：包大小预算。** 做 MTU/MSS/分片实验，解释为什么小包正常而大包卡住，为 UDP 留出实际封装空间。
4. **P0：公网可达性。** 理解 NAT、STUN/TURN/ICE，检查 Dedicated Server 的绑定、路由、安全组和防火墙；P1 补上 DNS 记录与服务发现。
5. **P0：TLS 与可信边界。** 能看证书验证失败，理解编码/压缩/加密的区别，验证输入、限流、幂等和防重放，不信任客户端上报的结算结果。
6. **P1：服务与连接的完整流程。** 画出登录、房间、匹配、验票、游戏连接与会话恢复时序，说明 HTTP/WebSocket/游戏传输如何分工。
7. **P0：弱网矩阵。** 用 clumsy、netem 或适用工具测试延迟、抖动、丢包和断线，验证预测、插值、校正；P1 补上重连令牌和网络切换实验。
8. **P0：可观测性与综合验收。** 用对局 ID 和请求 ID 关联日志、指标与 trace，在独立 Dedicated Server 环境定位一次真实问题并复测。

# 补充后总检查清单

- [ ] 能否用 `ss` 看 TCP 监听、UDP 绑定和已有连接，并找出对应进程？
- [ ] 能否用 `tcpdump` 抓 DNS/TCP/HTTP/UDP/TLS，并在 Wireshark 中初步看懂，说明加密后的可见边界？
- [ ] 能否解释 NAT 四类传统模型及其局限，以及 STUN、TURN、ICE、UPnP 的用途和穿透失败后的中继方案？
- [ ] 能否解释 Nagle、延迟 ACK、TIME_WAIT、Keepalive，以及小包延迟和大量短连接的排查方向？
- [ ] 能否解释 MTU、MSS、IP 分片和路径 MTU 黑洞，给出 UDP 包大小预算？
- [ ] 能否解释 TLS 握手失败，检查域名、SNI、ALPN、证书链、有效期、信任库和系统时间？
- [ ] 能否模拟高延迟和丢包，并进一步测试抖动、断线、重连，记录单向注入与实际 RTT 的区别？
- [ ] 能否区分服务没启动、端口没开、DNS 出错、网络不通、请求超时，并提供命令、日志或抓包证据？
- [ ] 能否为多人技能画出客户端输入、Server RPC、服务器校验、状态同步、各客户端表现？
- [ ] 能否独立完成 TCP/UDP echo，正确处理 TCP 消息边界、部分收发、超时和资源释放？
- [ ] 能否解释 A、AAAA、CNAME、TXT、SRV、缓存与 TTL，并确认客户端是否实际支持 SRV？
- [ ] 能否为登录、公告、聊天和实时状态选择 HTTP、WebSocket、TCP、UDP 或 QUIC，并解释代价？
- [ ] 能否画出公网玩家到 Dedicated Server 的路径，核对 VPC、子网、安全组、NAT 网关与负载均衡的职责？
- [ ] 能否区分序列化、压缩、加密、HMAC 和签名，验证非法输入、重复请求、过期票据和速率限制？
- [ ] 能否解释房间、账号会话、对局和网络连接的区别，以及客户端如何发现并加入游戏服？
- [ ] 能否说明断线后哪些状态由服务器保留、哪些需要重新同步，防止重复玩家、旧连接控制和重复结算？
- [ ] 能否在独立 Dedicated Server 环境，通过日志、RTT/丢包指标、UE trace 和服务器性能数据复现并定位问题？
- [ ] 能否明确区分“已由实验验证”“仅有设计方案”和“环境不足尚未验证”，不把内网成功当成公网已验收？

学习边界：物理层信号、编码、香农/奈奎斯特，链路层 CRC、CSMA/CD、PPP、STP、VLAN，RIP/OSPF/BGP 算法推导，IPv6 深度子网计算，FTP/SMTP/POP3/IMAP，无线蜂窝细节，TCP 拥塞控制公式和加密数学均不作为这条路线的重点；除直接影响当前联机排错的部分外，遇到再查。
